package customtransfer_test

import (
	"testing"

	// "github.com/stretchr/testify/require"
	// // keepertest "mudchain/testutil/keeper"
	// // "mudchain/testutil/nullify"
	// "github.com/evmos/evmos/v6/x/customtransfer"
	// "github.com/evmos/evmos/v6/x/customtransfer/types"
)

func TestGenesis(t *testing.T) {
	// genesisState := types.GenesisState{
	// 	Params: types.DefaultParams(),
	// 	PortId: types.PortID,
	// 	// this line is used by starport scaffolding # genesis/test/state
	// }

	// k, ctx := keepertest.CustomtransferKeeper(t)
	// customtransfer.InitGenesis(ctx, *k, genesisState)
	// got := customtransfer.ExportGenesis(ctx, *k)
	// require.NotNil(t, got)

	// nullify.Fill(&genesisState)
	// nullify.Fill(got)

	// require.Equal(t, genesisState.PortId, got.PortId)

	// this line is used by starport scaffolding # genesis/test/assert
}
